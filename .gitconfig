[user]
    email = ayumitamai97@gmail.com
    name = ayumitamai97

[filter "lfs"]
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f

[core]
    pager = delta --side-by-side --line-numbers
    editor = "nvim"

[delta]
    plus-color = "#012800"
    minus-color = "#340001"
    syntax-theme = Monokai Extended

[interactive]
    diffFilter = delta --color-only
